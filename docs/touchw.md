# `touchw`

Simulate Unix `touch` on Windows.

## Usage

When the destination doesn't exist, `touchw` will create an empty text file there:

```
> touchw path\to\dest.txt
```

In contrary, when the destination exists, `wouchw` just view it with `dir` in the background.
