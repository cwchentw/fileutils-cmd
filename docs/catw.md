# `catw`

`catw` simulates Unix `cat` on Windows.

## Usage

Concatenate one more text files together:

```
> catw path\to\a.txt path\to\b.txt path\to\c.txt
```

Maybe redirect the output to a new text file:

```
> catw a.txt b.txt c.txt > output.txt
```
